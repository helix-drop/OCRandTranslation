# Known Python-Rust Parity Diffs (Biopolitics Phase 1)

Auto-generated by `test_biopolitics_parity.rs` field-by-field comparison.
Classification: (a) Rust gap → Rust missing/fixable, (b) Python likely-bug → Python may be wrong.

---

## 1. page_partition role diffs

**Severity**: Note pages RESOLVED (100% match). Remaining: front_matter over-classification.

### 1a. ✅ RESOLVED: body→note (was 43 pages, now 0)

**Root cause**: `build_page_partitions` used `page_markdown_text(&page.pruned_result)`
to extract text for continuation checks, but Biopolitics fixtures store markdown at
the top-level `page.markdown` field, not inside `prunedResult`. All `page_texts` were
empty strings → `looks_like_note_continuation_page` always returned false → 43
`note_continuation` pages were missed.

**Fix** (2026-05-16): Changed `page_partition/mod.rs:35` to read from
`page.enriched_markdown.unwrap_or(&page.markdown)` instead of
`page_markdown_text(&page.pruned_result)`.

**Result**: Note pages Rust 62/62 Python (100%).

### 1b. Rust gap: front_matter over-classification (Rust 25 vs Python 5)

Rust classifies pages 1,4,7,13-29 as `front_matter` instead of `body`. Python has only
5 front_matter pages (8-12). Root cause: `apply_front_matter_continuation_fix` overshoots
because `is_body_entry_page` doesn't detect the Chapter 1 heading at page 17 as a strong
body boundary, so `in_front_matter_run` continues past page 17.

**Impact**: Ch1 start_page shifts from 17→30 (13-page cascade). This is the sole cause
of the remaining 20 role diffs.

**Status**: Known, fix pending (adjust `is_strong_body_boundary` or `early_limit`).

---

## 2. heading_candidates diffs

**Severity**: Low. Golden file has 103 heading candidates (generated without `--pdf`).

- `markdown_heading`: 31
- `ocr_block`: 48
- `visual_toc`: 12
- `note_heading`: 12
- `pdf_font_band`: 0 (no PDF available during golden generation)

### 2a. Rust gap: pdf_font_band

The `collect_pdf_font_band_candidates` function was a stub returning `vec![]` but
has been wired (2025-05-15) to call `extract_font_candidates` via pdfium-render.
Requires a PDF file at runtime to produce actual candidates.

### 2b. Rust gap: heading_candidates pipeline wired but no PDF

`toc_structure.rs:49-58` has wired `collect_heading_candidate_rows` into the builder
(via `Phase1Output`), producing markdown_heading/ocr_block/visual_toc candidates.
`pdf_font_band` candidates are absent because golden generation ran without `--pdf`.

---

## 3. Chapter boundary diffs

**Severity**: Medium (domino effect from §1 page_role gap).

12 chapters always detected, but start_page/end_page differ from Python golden because
Rust classifies fewer pages as `note` → different `_trim_chapter_rows` / `_infer_back_matter_start_page`.

| Chapter | Field | Rust | Python | Delta |
|---------|-------|------|--------|-------|
| Ch4 | start_page | 90 | 91 | -1 |
| Ch4 | end_page | 106 | 108 | -2 |
| Ch5 | start_page | 107 | 111 | -4 |
| Ch5 | end_page | 129 | 131 | -2 |
| Ch6 | start_page | 130 | 136 | -6 |  
| Ch7 | start_page | 149 | 165 | -16 |
| Ch7 | end_page | 164 | 178 | -14 |
| Ch8 | start_page | 165 | 179 | -14 |
| Ch8 | end_page | 191 | 196 | -5 |
| Ch9 | start_page | 192 | 203 | -11 |
| Ch9 | end_page | 218 | 232 | -14 |
| Ch10 | start_page | 219 | 233 | -14 |
| Ch11 | start_page | 252 | 257 | -5 |
| Ch11 | end_page | 289 | 301 | -12 |
| Ch12 | start_page | 290 | 307 | -17 |
| Ch12 | end_page | 355 | 349 | +6 |

**Root cause**: Python's 62 note pages cause `back_matter_start_page` detection and
`_trim_chapter_rows` to behave differently — chapters that overlap with long endnote
blocks are trimmed more aggressively. This is a cascade from §1 and not a separate bug.

**Resolution**: Fixing §1 (page_role parity) will resolve this automatically.

---

## 4. post_body_titles diffs

**Severity**: Low. Golden file has empty `post_body_titles` (generated without `visual_toc_bundle`).

The Rust `Phase2Input` now accepts `post_body_titles: HashSet<String>` and
`build_note_regions` passes it through to `reclassify_post_body_fnblocks`.
When populated from Phase1Summary, this will enable post-body fnBlock → endnote
reclassification (see note_regions/post_body_promote.rs).

No diff to report since both sides have empty post_body_titles currently.

---

## 5. Summary

| Metric | Rust | Python | Coverage |
|--------|------|--------|----------|
| Total pages | 370 | 370 | 100% |
| **note pages** | **62** | **62** | **100%** ✅ |
| other pages | 14 | 15 | 93% |
| noise pages | 3 | 3 | 100% |
| body pages | 266 | 285 | 93% |
| front_matter pages | 25 | 5 | — (over-classification) |
| Chapters | 12 | 12 | 100% |
| Role agreement | 324/370 | — | 87.6% |

**Next steps**:
1. Fix front_matter over-classification (pages 1,4,7,13-29) — `is_body_entry_page` for Ch1
2. Regenerate golden → chapter boundary diffs will auto-resolve
3. Investigate Phase 2 footnote band gap (10 vs 62 footnote regions)
